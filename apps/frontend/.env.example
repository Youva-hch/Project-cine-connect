# Copier vers `.env.local` pour le dev local (fichier ignoré par git).
# Ne pas définir VITE_API_URL en production : le front appelle `/api/...` sur le même domaine (Upsun).

# Optionnel : backend local sur le port 3000
# VITE_API_URL=http://localhost:3000
